# Claude Code local state and worktrees (machine-specific, never shared)
.claude/

# Third-party addons vendored locally as mocking/reference aids (~247 MB).
# Not ours to redistribute.
other_addons/

# Scratch debug dumps and captured error logs. The rest of thoughts/ IS tracked
# on the dev branch and stripped from release branches by scripts/make-release.sh.
thoughts/debug/
thoughts/errors/
